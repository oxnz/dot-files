# ~/.shrc

for i in ~/.shell/*.{ba,}sh; do
	if [ -f $i -a -r $i ]; then
		. $i
	fi
done

unset i

# show banner
# sed -e "s/.\{20\}/&\n/g"
cat <<End-Of-Info | sed -e 's/.\{80\}/&\n/g'
Mail: $(/usr/sbin/sendmail -bp)
Memory:
$(free -h)
Crontab:
M H D m W command
$(crontab -l 2>&1)
End-Of-Info

cat <<"End-Of-Cow"
        \   ^__^
         \  (oo)\_______
            (__)\       )\/\
                ||----w |
                ||     ||
End-Of-Cow
:
